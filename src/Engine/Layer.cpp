// ProgrammerLayer moved to include/LightEngine/Engine/Programmer.h (header-only,
// now that it composes static edits + dynamic effects inline). This translation
// unit is intentionally empty; kept so existing build globs stay happy.
